name: Pylint

on:
  push:
    paths:
      - '**/*.py'
  pull_request:
    paths:
      - '**/*.py'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install Poetry
        run: pip install poetry

      - name: Install dependencies with Poetry
        working-directory: viikko2/nhl-reader
        run: poetry install

      - name: Run Pylint
        working-directory: viikko2/nhl-reader
        run: poetry run pylint src

